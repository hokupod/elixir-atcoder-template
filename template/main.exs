defmodule Main do
  def solve do
    # Example: reading one line from standard input
    # line = IO.read(:line) |> String.trim()

    # Example: reading two integers separated by spaces
    # [a, b] = IO.read(:line) |> String.trim() |> String.split(" ") |> Enum.map(&String.to_integer/1)

    IO.puts "Hello from Elixir Template!"
  end
end

Main.solve()
